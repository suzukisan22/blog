<%= form_for(@poem) do |f| %>
  <% if @poem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@poem.errors.count, "error") %> prohibited this poem from being saved:</h2>

      <ul>
      <% @poem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.radio_button :status, 1 %><%= f.label :status_1, "下書き" %>
    <%= f.radio_button :status, 2 %><%= f.label :status_2, "公開" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
